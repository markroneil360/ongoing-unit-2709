name: Manual R6E8A dashboard verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-static-dashboard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install report verification dependencies
        run: python -m pip install --disable-pip-version-check reportlab pypdf
      - name: Verify percentage-only public output
        run: python scripts/sanity_check.py
